const Game = () => {
    const sendMessage = () => {
        
    }

    return (
        <section>
            <input type="text" placeholder="Message..." />
            <button onClick={sendMessage}>Send Message</button>
        </section>
    )
}

export default Game;